// Todo /* ========== Mixitup : Filter portfolio ========== */

// ? Example: Passing a configuration object
var mixerFilters = mixitup('.work_container', {
  selectors: {
    target: '.card',
  },
  animation: {
    duration: 300,
  },
});
